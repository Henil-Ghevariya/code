#include <stdio.h>
int main()
{
 float a,b,c;
 printf("enter three different value:");
 scanf("%f %f %f",&a,&b,&c);
 if(a>b && b>c)
  {
   printf("the largest value is %f\nthe smallest value is %f",a,c);
  }
 else
 if(a>c && c>b)
  {
   printf("the largest value is %f\nthe smallest value is %f",a,b);
  }
 else
 if(b>a && a>c)
  {
   printf("the largest value is %f\nthe smallest value is %f",b,c);
  } 
 else
 if(b>c && c>a)
  {
   printf("the largest value is %f\nthe smallest value is %f",b,a);
  }
 else
 if(c>a && a>b)
  {
   printf("the largest value is %f\nthe smallest value is %f",c,b);
  }
 else
  {
   printf("the largest value is %f\nthe smallest value is %f",c,a);
  };
 return 0;
} 